import { infoWebsiteSchema, infoMenuSchema, slideSchema } from "./websiteSchema";
import { postDetailSchema, postListSchema, authorPostsSchema, pageSchema } from "./postSchema";
import { categorySchema, categoryListSchema } from "./categorySchema";
import { eventSchema } from "./eventSchema";
import { galleryPhotoCategorySchema, galleryPhotoByCategorySchema, galleryPhotoDetailByCategorySchema } from "./gallerySchema";

export { 
    infoWebsiteSchema, 
    infoMenuSchema, 
    slideSchema,
    pageSchema, 
    postListSchema, 
    postDetailSchema, 
    authorPostsSchema,
    categorySchema,
    categoryListSchema,
    eventSchema,
    galleryPhotoCategorySchema,
    galleryPhotoByCategorySchema,
    galleryPhotoDetailByCategorySchema
};
---
// Brochure Request Form Component
import { websiteInfo } from "@globals/info";

const studyPrograms = [
    "S1 Manajemen (S.M.)",
    "S1 Akuntansi (S.Ak.)",
    "S1 Bisnis (S.Bns.)",
    "S1 Hukum (S.H.)",
    "S1 Teknik Sipil (S.T.)",
    "S1 Teknik Informatika (S.Kom.)",
    "S1 Desain Komunikasi Visual (S.Ds.)",
    "S1 Sistem Komputer (S.Kom.)",
    "S1 Sistem Informasi (S.Kom.)",
    "D4 Akuntansi Perpajakan (S.Tr.Ak.)",
    "D3 Teknik Informatika (A.Md.Kom.)",
];

const studyMethods = ["Full Online", "Online Offline (Hybrid)", "Offline"];
const brochureLink = websiteInfo.brosur.pmb;
---

<div
    class="bg-white dark:bg-gray-800 rounded-xl shadow-md border border-gray-200 dark:border-gray-700 overflow-hidden"
>
    <!-- Header -->
    <div class="bg-gradient-to-r from-orange-500 to-orange-600 p-3">
        <h3 class="text-base font-bold text-white">Permintaan Brosur</h3>
    </div>

    <!-- Form -->
    <div class="p-4">
        <form id="brochure-form" class="space-y-3">
            <!-- Nama Lengkap -->
            <div>
                <label
                    for="fullName"
                    class="block text-xs font-semibold text-[#1055C9] dark:text-blue-400 mb-1"
                >
                    Nama Lengkap <span class="text-red-500">*</span>
                </label>
                <input
                    type="text"
                    id="fullName"
                    name="fullName"
                    required
                    minlength="5"
                    class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-[#1055C9] focus:border-transparent dark:bg-gray-700 dark:text-white transition-all"
                    placeholder="Masukkan nama lengkap (min. 5 karakter)"
                />
                <p id="fullName-error" class="hidden text-xs text-red-500 mt-1">
                </p>
            </div>

            <!-- Email -->
            <div>
                <label
                    for="email"
                    class="block text-xs font-semibold text-[#1055C9] dark:text-blue-400 mb-1"
                >
                    Email <span class="text-red-500">*</span>
                </label>
                <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-[#1055C9] focus:border-transparent dark:bg-gray-700 dark:text-white transition-all"
                    placeholder="email@example.com"
                />
                <p id="email-error" class="hidden text-xs text-red-500 mt-1">
                </p>
            </div>

            <!-- No HP -->
            <div>
                <label
                    for="phone"
                    class="block text-xs font-semibold text-[#1055C9] dark:text-blue-400 mb-1"
                >
                    No HP Anda <span class="text-red-500">*</span>
                </label>
                <input
                    type="tel"
                    id="phone"
                    name="phone"
                    required
                    pattern="^(62|08)\d{10,}$"
                    class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-[#1055C9] focus:border-transparent dark:bg-gray-700 dark:text-white transition-all"
                    placeholder="62xxx atau 08xxx (min. 12 digit)"
                />
                <p id="phone-error" class="hidden text-xs text-red-500 mt-1">
                </p>
            </div>

            <!-- Alamat Domisili -->
            <div>
                <label
                    for="address"
                    class="block text-xs font-semibold text-[#1055C9] dark:text-blue-400 mb-1"
                >
                    Alamat Domisili <span class="text-red-500">*</span>
                </label>
                <textarea
                    id="address"
                    name="address"
                    required
                    minlength="10"
                    rows="2"
                    class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-[#1055C9] focus:border-transparent dark:bg-gray-700 dark:text-white transition-all resize-none"
                    placeholder="Masukkan alamat lengkap (min. 10 karakter)"
                ></textarea>
                <p id="address-error" class="hidden text-xs text-red-500 mt-1">
                </p>
            </div>

            <!-- Pilihan Jurusan -->
            <div>
                <label
                    for="studyProgram"
                    class="block text-xs font-semibold text-[#1055C9] dark:text-blue-400 mb-1"
                >
                    Pilihan Jurusan <span class="text-red-500">*</span>
                </label>
                <select
                    id="studyProgram"
                    name="studyProgram"
                    required
                    class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-[#1055C9] focus:border-transparent dark:bg-gray-700 dark:text-white transition-all appearance-none bg-no-repeat bg-right pr-8"
                    style="background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27currentColor%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-size: 1.2em;"
                >
                    <option value="" disabled selected>Pilih jurusan</option>
                    {
                        studyPrograms.map((program) => (
                            <option value={program}>{program}</option>
                        ))
                    }
                </select>
            </div>

            <!-- Minat Metode Perkuliahan -->
            <div>
                <label
                    for="studyMethod"
                    class="block text-xs font-semibold text-[#1055C9] dark:text-blue-400 mb-1"
                >
                    Minat Metode Perkuliahan <span class="text-red-500">*</span>
                </label>
                <select
                    id="studyMethod"
                    name="studyMethod"
                    required
                    class="w-full px-3 py-2 text-sm border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-[#1055C9] focus:border-transparent dark:bg-gray-700 dark:text-white transition-all appearance-none bg-no-repeat bg-right pr-8"
                    style="background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27currentColor%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-size: 1.2em;"
                >
                    <option value="" disabled selected>Pilih metode</option>
                    {
                        studyMethods.map((method) => (
                            <option value={method}>{method}</option>
                        ))
                    }
                </select>
            </div>

            <!-- Submit Button -->
            <button
                type="submit"
                class="w-full px-4 py-2.5 bg-green-600 hover:bg-green-700 text-white font-bold text-sm rounded-lg transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
            >
                KIRIM
            </button>
        </form>
    </div>
</div>

<!-- Success Modal -->
<div id="brochure-modal" class="hidden fixed inset-0 z-50 overflow-y-auto">
    <!-- Backdrop -->
    <div class="fixed inset-0 bg-black/50 backdrop-blur-sm transition-opacity">
    </div>

    <!-- Modal Content -->
    <div class="flex min-h-full items-center justify-center p-4">
        <div
            class="relative bg-white dark:bg-gray-800 rounded-2xl shadow-2xl max-w-md w-full p-6 transform transition-all"
        >
            <!-- Success Icon -->
            <div
                class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-green-100 dark:bg-green-900/30 mb-4"
            >
                <svg
                    class="h-10 w-10 text-green-600 dark:text-green-400"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M5 13l4 4L19 7"></path>
                </svg>
            </div>

            <!-- Title -->
            <h3
                class="text-xl font-bold text-center text-gray-900 dark:text-white mb-2"
            >
                Terima Kasih!
            </h3>

            <!-- Message -->
            <p class="text-center text-gray-600 dark:text-gray-300 mb-6">
                Permintaan brosur Anda telah dikirim. Silakan download brosur
                kami melalui link di bawah ini.
            </p>

            <!-- Brochure Link Button -->
            <a
                id="brochure-link"
                href={brochureLink}
                target="_blank"
                rel="noopener noreferrer"
                class="block w-full px-6 py-3 bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white font-bold text-center rounded-lg transition-all duration-300 shadow-lg hover:shadow-xl mb-3"
            >
                <span class="flex items-center justify-center gap-2">
                    <svg
                        class="w-5 h-5"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                        ></path>
                    </svg>
                    Download Brosur
                </span>
            </a>

            <!-- Close Button -->
            <button
                id="close-modal"
                class="w-full px-6 py-2.5 bg-gray-200 dark:bg-gray-700 hover:bg-gray-300 dark:hover:bg-gray-600 text-gray-800 dark:text-gray-200 font-semibold rounded-lg transition-all duration-300"
            >
                Tutup
            </button>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        // Handle form submission
        const form = document.getElementById(
            "brochure-form",
        ) as HTMLFormElement;
        const modal = document.getElementById("brochure-modal");
        const closeModalBtn = document.getElementById("close-modal");
        const brochureLink =
            document.getElementById("brochure-link")?.getAttribute("href") ||
            "";

        // Get form fields
        const fullNameInput = document.getElementById(
            "fullName",
        ) as HTMLInputElement;
        const emailInput = document.getElementById("email") as HTMLInputElement;
        const phoneInput = document.getElementById("phone") as HTMLInputElement;
        const addressInput = document.getElementById(
            "address",
        ) as HTMLTextAreaElement;

        // Get error message elements
        const fullNameError = document.getElementById("fullName-error");
        const emailError = document.getElementById("email-error");
        const phoneError = document.getElementById("phone-error");
        const addressError = document.getElementById("address-error");

        console.log("Script loaded, form:", form, "modal:", modal);

        // Validation functions
        function validateFullName(): boolean {
            const value = fullNameInput.value.trim();
            if (value.length < 5) {
                showError(
                    fullNameInput,
                    fullNameError,
                    "Nama harus minimal 5 karakter",
                );
                return false;
            }
            clearError(fullNameInput, fullNameError);
            return true;
        }

        function validateAddress(): boolean {
            const value = addressInput.value.trim();
            if (value.length < 10) {
                showError(
                    addressInput,
                    addressError,
                    "Alamat harus minimal 10 karakter",
                );
                return false;
            }
            clearError(addressInput, addressError);
            return true;
        }

        function validatePhone(): boolean {
            const value = phoneInput.value.trim();

            // Check if it's all numbers
            if (!/^\d+$/.test(value)) {
                showError(
                    phoneInput,
                    phoneError,
                    "Nomor HP harus berupa angka",
                );
                return false;
            }

            // Check if starts with 62 or 08
            if (!value.startsWith("62") && !value.startsWith("08")) {
                showError(
                    phoneInput,
                    phoneError,
                    "Nomor HP harus diawali dengan 62 atau 08",
                );
                return false;
            }

            // Check minimum length (12 digits)
            if (value.length < 12) {
                showError(
                    phoneInput,
                    phoneError,
                    "Nomor HP harus minimal 12 digit",
                );
                return false;
            }

            clearError(phoneInput, phoneError);
            return true;
        }

        function showError(
            input: HTMLInputElement | HTMLTextAreaElement,
            errorElement: HTMLElement | null,
            message: string,
        ) {
            input.classList.add("border-red-500", "dark:border-red-500");
            input.classList.remove("border-gray-300", "dark:border-gray-600");
            if (errorElement) {
                errorElement.textContent = message;
                errorElement.classList.remove("hidden");
            }
        }

        function clearError(
            input: HTMLInputElement | HTMLTextAreaElement,
            errorElement: HTMLElement | null,
        ) {
            input.classList.remove("border-red-500", "dark:border-red-500");
            input.classList.add("border-gray-300", "dark:border-gray-600");
            if (errorElement) {
                errorElement.textContent = "";
                errorElement.classList.add("hidden");
            }
        }

        // Real-time validation
        if (fullNameInput) {
            fullNameInput.addEventListener("blur", validateFullName);
            fullNameInput.addEventListener("input", () => {
                if (fullNameInput.value.length >= 5) {
                    clearError(fullNameInput, fullNameError);
                }
            });
        }

        if (phoneInput) {
            phoneInput.addEventListener("blur", validatePhone);
            phoneInput.addEventListener("input", () => {
                // Clear error as user types if validation passes
                const value = phoneInput.value.trim();
                if (
                    value.length >= 12 &&
                    /^\d+$/.test(value) &&
                    (value.startsWith("62") || value.startsWith("08"))
                ) {
                    clearError(phoneInput, phoneError);
                }
            });
        }

        if (addressInput) {
            addressInput.addEventListener("blur", validateAddress);
            addressInput.addEventListener("input", () => {
                if (addressInput.value.trim().length >= 10) {
                    clearError(addressInput, addressError);
                }
            });
        }

        if (form && modal) {
            form.addEventListener("submit", async (e) => {
                e.preventDefault();
                console.log("Form submitted!");

                // Validate all fields
                const isFullNameValid = validateFullName();
                const isPhoneValid = validatePhone();
                const isAddressValid = validateAddress();

                if (!isFullNameValid || !isPhoneValid || !isAddressValid) {
                    console.log("Validation failed");
                    return;
                }

                const formData = new FormData(form);
                const data = Object.fromEntries(formData.entries());

                // You can add your API call here
                console.log("Form data:", data);

                // Show modal
                modal.classList.remove("hidden");
                console.log("Modal should be visible now");

                // Reset form
                form.reset();

                // Clear all errors
                clearError(fullNameInput, fullNameError);
                clearError(emailInput, emailError);
                clearError(phoneInput, phoneError);
                clearError(addressInput, addressError);
            });
        } else {
            console.error("Form or modal not found!", { form, modal });
        }

        // Close modal handlers
        if (closeModalBtn && modal) {
            closeModalBtn.addEventListener("click", () => {
                modal.classList.add("hidden");
            });

            // Close on backdrop click
            modal.addEventListener("click", (e) => {
                if (e.target === modal) {
                    modal.classList.add("hidden");
                }
            });

            // Close on Escape key
            document.addEventListener("keydown", (e) => {
                if (e.key === "Escape" && !modal.classList.contains("hidden")) {
                    modal.classList.add("hidden");
                }
            });
        }
    });
</script>

<style>
    /* Custom select arrow color for dark mode */
    select option {
        background-color: white;
        color: black;
    }

    .dark select option {
        background-color: rgb(55 65 81);
        color: white;
    }
</style>
